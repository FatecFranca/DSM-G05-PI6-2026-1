version: "3.9"

# ============================================================
# docker-compose para Backend (NestJS) + Frontend (Next.js)
#
# Pré-requisitos na VM:
#   - PostgreSQL rodando em 201.54.22.62:5432
#   - Python API rodando em 201.54.22.62:8000
#
# Uso:
#   docker compose up -d --build
# ============================================================

services:

  # ── Redis (exigido pelo BullMQ no NestJS) ────────────────
  redis:
    image: redis:7-alpine
    container_name: commodities_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Backend NestJS ────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: commodities_backend
    restart: unless-stopped
    ports:
      - "9000:9000"
    depends_on:
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 9000
      # Redis (BullMQ)
      REDIS_HOST: redis
      REDIS_PORT: 6379
      # Auth
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-30m}
      # Banco de dados (já na VM)
      DATABASE_URL: ${DATABASE_URL}
      # API Python (já na VM)
      EXTERNAL_API_URL: ${EXTERNAL_API_URL:-http://201.54.22.62:8000}
      # Gemini
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      # SMTP
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      SMTP_FROM: ${SMTP_FROM}
      # Telegram (opcional)
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
    healthcheck:
      # GET /api/v1/ → AppController retorna 200 "Hello World!"
      test: ["CMD-SHELL", "wget -qO- http://localhost:9000/ 2>&1 | grep -q . || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # ── Frontend Next.js ──────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://201.54.22.62:9000/api/v1}
    container_name: commodities_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    environment:
      NODE_ENV: production
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000 2>&1 | grep -q . || exit 1"]
      interval: 20s
      timeout: 10s
      retries: 3
      start_period: 30s
